@page
@model BrgyLink.Pages.ManageResident.CreateModel

@{
    ViewData["Title"] = "Create Resident";
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
}

<h4 class="text-center mb-3 text-primary">Create Resident</h4>
<form method="post" enctype="multipart/form-data" id="createResidentForm">
    <div asp-validation-summary="ModelOnly" class="text-danger mb-2"></div>
<div class="container">
    <div class="row justify-content-center">
        <!-- Image Upload Column -->
        <div class="col-12 col-md-4 mb-3">
            <div class="card shadow-sm">
                <div class="card-header py-2 bg-primary text-white">Image Upload</div>
                <div class="card-body p-2 text-center">
                    <img id="imagePreview" src="/images/avatarPlaceholder.jpg" alt="Resident Image Preview"
                         class="img-fluid rounded-circle mx-auto d-block" style="width: 150px; height: 150px; object-fit: cover;" />
                    <div class="mt-2">
                        <input type="file" class="form-control form-control-sm" asp-for="Resident.ImageFile"
                               accept=".jpg,.jpeg,.png" onchange="previewImage(this)" />
                        <small class="text-muted">Max: 5MB (JPG, JPEG, PNG)</small>
                    </div>
                </div>
            </div>
        </div>

        <!-- Form Column -->
        <div class="col-12 col-md-8">


                <!-- Name Section -->
                <div class="card mb-3 shadow-sm">
                    <div class="card-header py-1 bg-secondary text-white">Name</div>
                    <div class="card-body p-2">
                        <div class="row g-2">
                            <div class="col-sm-6">
                                <label class="form-label mb-1">First Name</label>
                                <input type="text" class="form-control form-control-sm" placeholder="First Name" asp-for="Resident.FirstName" required />
                                <span asp-validation-for="Resident.FirstName" class="text-danger"></span>
                            </div>
                            <div class="col-sm-6">
                                <label class="form-label mb-1">Last Name</label>
                                <input type="text" class="form-control form-control-sm" placeholder="Last Name" asp-for="Resident.LastName" required />
                                <span asp-validation-for="Resident.LastName" class="text-danger"></span>
                            </div>
                            <div class="col-12">
                                <label class="form-label mb-1">Middle Name</label>
                                <input type="text" class="form-control form-control-sm" placeholder="Middle Name" asp-for="Resident.MiddleName" />
                                <span asp-validation-for="Resident.MiddleName" class="text-danger"></span>
                                <small class="text-muted">If no Middle Name put "None"</small>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Personal Information -->
                <div class="card mb-3 shadow-sm">
                    <div class="card-header py-1 bg-secondary text-white">Personal Information</div>
                    <div class="card-body p-2">
                        <div class="row g-2">
                            <div class="col-sm-4">
                                <label class="form-label mb-1">Birthdate</label>
                                <input type="date" class="form-control form-control-sm" asp-for="Resident.BirthDate" required />
                                <span asp-validation-for="Resident.BirthDate" class="text-danger"></span>
                            </div>
                            <div class="col-sm-4">
                                <label class="form-label mb-1">Gender</label>
                                <select class="form-select form-select-sm" asp-for="Resident.Gender" required>
                                    <option value="">Select</option>
                                    <option value="Male">Male</option>
                                    <option value="Female">Female</option>
                                    <option value="Other">Other</option>
                                </select>
                                <span asp-validation-for="Resident.Gender" class="text-danger"></span>
                            </div>
                            <div class="col-sm-4">
                                <label class="form-label mb-1">Civil Status</label>
                                <select class="form-select form-select-sm" asp-for="Resident.CivilStatus" required>
                                    <option value="Single">Single</option>
                                    <option value="Married">Married</option>
                                    <option value="Divorced">Divorced</option>
                                    <option value="Widowed">Widowed</option>
                                </select>
                                <span asp-validation-for="Resident.CivilStatus" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Contact Information -->
                <div class="card mb-3 shadow-sm">
                    <div class="card-header py-1 bg-secondary text-white">Contact Information</div>
                    <div class="card-body p-2">
                        <div class="row g-2">
                            <div class="col-12">
                                <label class="form-label mb-1">Address</label>
                                <textarea class="form-control form-control-sm" rows="1" placeholder="Enter Address" asp-for="Resident.Address" required></textarea>
                                <span asp-validation-for="Resident.Address" class="text-danger"></span>
                            </div>
                            <div class="col-sm-6">
                                <label class="form-label mb-1">Contact Number</label>
                                <input type="tel" class="form-control form-control-sm" placeholder="Enter Contact Number" asp-for="Resident.ContactNumber" required />
                                <span asp-validation-for="Resident.ContactNumber" class="text-danger"></span>
                            </div>
                            <div class="col-sm-6">
                                <label class="form-label mb-1">Email</label>
                                <input type="email" class="form-control form-control-sm" placeholder="Enter Email" asp-for="Resident.Email" />
                                <span asp-validation-for="Resident.Email" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Special Information -->
                <div class="card mb-3 shadow-sm">
                    <div class="card-header py-1 bg-secondary text-white">Special Information</div>
                    <div class="card-body p-2">
                        <div class="row g-2">
                            <div class="col-sm-4">
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" asp-for="Resident.IsSeniorCitizen" />
                                    <label class="form-check-label">Senior Citizen</label>
                                </div>
                                <div class="form-check">
                                    <input class="form-check-input" type="checkbox" asp-for="Resident.IsPWD" />
                                    <label class="form-check-label">PWD</label>
                                </div>
                            </div>
                            <div class="col-sm-4">
                                <label class="form-label mb-1">Voter Status</label>
                                <select class="form-select form-select-sm" asp-for="Resident.VoterStatus" required>
                                    <option value="Non-voter">Non-voter</option>
                                    <option value="Voter">Voter</option>
                                </select>
                                <span asp-validation-for="Resident.VoterStatus" class="text-danger"></span>
                            </div>
                            <div class="col-sm-4">
                                <label class="form-label mb-1">Occupation</label>
                                <input type="text" class="form-control form-control-sm" placeholder="Enter Occupation" asp-for="Resident.Occupation" />
                                <span asp-validation-for="Resident.Occupation" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Emergency Contact -->
                <div class="card mb-3 shadow-sm">
                    <div class="card-header py-1 bg-secondary text-white">Emergency Contact</div>
                    <div class="card-body p-2">
                        <div class="row g-2">
                            <div class="col-sm-6">
                                <label class="form-label mb-1">Emergency Contact</label>
                                <input type="text" class="form-control form-control-sm" placeholder="Enter Emergency Contact" asp-for="Resident.EmergencyContact" required />
                                <span asp-validation-for="Resident.EmergencyContact" class="text-danger"></span>
                            </div>
                            <div class="col-sm-6">
                                <label class="form-label mb-1">Health Conditions</label>
                                <textarea class="form-control form-control-sm" rows="1" placeholder="Enter Health Conditions" asp-for="Resident.HealthConditions"></textarea>
                                <span asp-validation-for="Resident.HealthConditions" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Submit Button -->
                <div class="text-end mb-3">
                    <button type="submit" class="btn btn-primary">Save Resident</button>
                </div>
         
        </div>
    </div>
</form>
</div>





@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        function previewImage(input) {
            if (input.files && input.files[0]) {
                const file = input.files[0];
                if (file.size > 5 * 1024 * 1024) {
                    alert('File size cannot exceed 5MB');
                    input.value = '';
                    return;
                }
                const reader = new FileReader();
                reader.onload = function (e) {
                    document.getElementById('imagePreview').src = e.target.result;
                };
                reader.readAsDataURL(file);
            }
        }

        @if (TempData["SuccessMessage"] != null)
        {
            <text>
                $(document).ready(function () {
                    Swal.fire({
                        title: 'Success',
                        text: 'Resident created successfully!',
                        icon: 'success',
                        confirmButtonText: 'OK'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            $('#createResidentForm')[0].reset();
                            $('#imagePreview').attr('src', '/images/avatarPlaceholder.jpg');
                        }
                    });
                });
            </text>
        }
    </script>
}