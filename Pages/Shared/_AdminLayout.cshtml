@using Microsoft.AspNetCore.Identity

@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css">
    <link href="https://fonts.googleapis.com/css2?family=Candara:wght@400&display=swap" rel="stylesheet"> <!-- Include Candara font -->
    <link rel="stylesheet" href="~/css/sidebar-style.css" asp-append-version="true" />
    <title>@ViewData["Title"] - BrgyLink</title>
</head>
<body>
    <nav class="sidebar">
        <div class="sidebar-header">
            <h5>BrgyLink</h5>
        </div>
        <div class="nav-buttons d-flex flex-column">
            <a href="#" class="nav-link mb-0">
                <i class="bi bi-house-door"></i><span>Home</span>
            </a>
            <a href="#" class="nav-link mb-0">
                <i class="bi bi-person-badge"></i><span>Resident</span>
            </a>
            <a href="#" class="nav-link mb-0">
                <i class="bi bi-journal-text"></i><span>Blotter</span>
            </a>
        </div>
    </nav>

    <div class="header-content">
        <header>
            <button id="sidebarToggler" class="toggler-button">
                <i class="bi bi-list"></i> <!-- Navbar toggler icon -->
            </button>
            <div class="d-flex align-items-center">
                <!-- Align items for the right section -->
                <div class="dropdown me-3">
                    <!-- Dropdown for profile -->
                    <button class="btn btn-profile dropdown-toggle" type="button" id="profileDropdown" data-bs-toggle="dropdown" aria-expanded="false" style="border: none;">
                        <a id="manage" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
                    </button>

                    <ul class="dropdown-menu" aria-labelledby="profileDropdown">
                        <li><a class="dropdown-item" href="#">Manage Account</a></li>
                        <li><a class="dropdown-item" href="#">Logout</a></li>
                    </ul>
                </div>
                <button id="darkModeToggle" class="toggler-button">
                    <i class="bi bi-moon"></i> <!-- Dark mode toggler icon -->
                </button>
            </div>
        </header>

        <div class="content">
            <!-- Content area -->
            @RenderBody()
        </div>
    </div>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/layout-animations.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>